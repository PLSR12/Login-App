import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {},
  label: {
    fontSize: 20,
    color: '#000',
    fontWeight: 'bold',
  },
  input: {
    width: 150,
    borderWidth: 2,
    borderRadius: 10,
  },
})
